!!!
%html
  %head
    %meta{:charset => "utf-8"}
    
    %title Railscamp Ideagora
    -#= stylesheet_link_tag 'ideagora'
    = javascript_include_tag 'vendor/jquery.min'
    = csrf_meta_tag
    
    = yield :header
    
  %body{:id => @body_id || controller.controller_name, :class => controller.action_name }
    #wrapper
      %header#header
        -# something will come here later
      #content
        = flash_helper.html_safe
        = yield
